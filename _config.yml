title: BioMonth - CSC supercomputing and data management for bioscientists

plugins:
  - jekyll-sitemap


collections:
  hands-on:
    output: true
    permalink: /:path/

defaults:
  -
    scope:
      path: "_lectures"
    values:
      layout: "slides-csc"
  -
    scope:
      type: "_hands-on"
      path: "/connecting/"
    values:
      layout: "default"
  -
    scope:
      path: "_exercises"
    values:
      layout: "default"
